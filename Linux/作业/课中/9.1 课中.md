- **使用sed命令把/etc/selinux/config文件中的SELINUX=enforcing设置为SELINUX=disabled**（记得使用-i.bak）
```bash
[root@dpeak ~]# sed -i.bak '7s/enforcing/disabled/' /etc/selinux/config

```
- **使用sed在/etc/ssh/sshd_config文件的#Port 22这一行的后面新增Port 2222**（记得使用-i.bak）
```bash
[root@dpeak ~]# sed -i.bak '/^#Port/a\Port 2222' /etc/ssh/sshd_config
```
- **使用sed把/etc/ssh/sshd_config文件的#Port 22关键字删除掉**（记得使用-i.bak）
```bash
[root@dpeak ~]# sed -i.bak 's/#Port 22//'  /etc/ssh/sshd_config
```

---
---
# 变量定义、引用、取消:
切换到root用户身份满足以下需求：
1. 定义本地变量os version，值为rhel9，通过echo命令将其变量的值输出到屏幕上
```bash
[root@dpeak ~]# os_version=rhel9
[root@dpeak ~]# echo $os_version
rhel9
```
2. 再次定义变量os_version，值为rockylinux8，通过echo命令输出，观察输出的结果。同时新打开一个标签页，通过echo命令，观察输出结果
```bash
[root@dpeak ~]# os_version=rockylinux8
[root@dpeak ~]# echo $os_version
rockylinux8
###新开一个标签页
[root@dpeak ~]#  echo $os_version

```

3. 定义环境变量env_os=rockylinux9，打开子SHELL，在子SHELL中通过echo输出变量的值
```bash
[root@dpeak ~]# export env_os=rockylinux9
[root@dpeak ~]# env | grep env_os
env_os=rockylinux9
[root@dpeak ~]# bash
[root@dpeak ~]# echo $env_os
rockylinux9
```
  
4. 将本地变量os_version转化为环境变量
```bash
[root@dpeak ~]# export os_version

```
5. 通过env命令过滤os_version的信息
```bash
[root@dpeak ~]# env | grep os_version
```
6. 取消os_version变量的定义，再次通过echo命令输出变量的值，观察变化
```bash
[root@dpeak ~]# unset os_version
[root@dpeak ~]# echo $os_version
```

---
---
# alias案例练习：

准备工作：cp /etc/passwd /opt/
1.删除掉rm的别名，执行rm /opt/passwd 看看效果
```bash
[root@dpeak ~]# cp /etc/passwd /opt/
[root@dpeak ~]# unalias rm
[root@dpeak ~]# rm /opt/passwd

```


2.查看系统上Is命令的别名和ll命令的别名
```bash

alias ll='ls -l --color=auto'
alias ls='ls --color=auto'

```
3.自定义别名rmtmp，要求执行rmtmp之后可以清空/tmp目录下的所有的文件
```bash
[root@dpeak ~]# alias rmtmp='rm -rf /tmp/*'
```
4.定义别名cptime，要求执行cptime之后，可以
将/etc/passwd文件拷贝到/root/passwd-时-分-秒
```bash

[root@dpeak ~]# alias cptime='cp /etc/passwd /root/passwd-$(date +%T)'

```

5.重新定义rm别名，要求执行rm相当于执行rm -i
```bash
[root@dpeak ~]# alias rm='rm -i'

```
6.删除别名cptime和rmtmp工
```bash
[root@dpeak ~]# unalias cptime
[root@dpeak ~]# unalias rmtmp

```

---
---
# SHELL基础、变量、别名例题
## shell案例题
1. shell在系统中的作用是什么
```test
命令解释器
```

2. 如何查看系统支持哪些shell
```bash
[root@dpeak ~]# cat /etc/shells
/bin/sh
/bin/bash
/usr/bin/sh
/usr/bin/bash

```

3. 查看当前使用的shell
```bash
[root@dpeak ~]# echo $SHELL
/bin/bash

```

4. bash作为shell的一种，支持哪些功能和快捷键
   - 支持的功能：
	   - 查看历史记录
	```bash
	[root@dpeak ~]# history
	```
	- tab键补全
	- 重定向
	- 管道符
	- 通配符
- 快捷键：
	`ctrl c`：中断
	`ctrl z`：停止
	`ctrl u`：将光标前面的所有内容进行剪切
	`ctrl k`：将光标后面的所有内容进行剪切
	`ctrl w`：将光标前面的单词进行剪切
	`ctrl y`：将剪切的内容进行粘贴
	`ctrl a`：把光标跳转到行首
	`ctrl e`：把光标跳转到行尾
	`ctrl s`：暂停屏幕输出，但是你执行的命令是会执行的
	`ctrl q`：恢复屏幕输出
	`ctrl l`：清屏

5. history历史记录怎么干净的删除掉用户的所有记录
```bash
[root@dpeak ~]# rm -f .bash_history
[root@dpeak ~]# history -c

```
## 变量、别名
1.将/bin/true文件拷贝到/opt/command目录下，并且允许此目录下的命令文件可以像执行ls命令那样执行
```bash
[root@dpeak ~]# cp /bin/true /opt/command/
[root@dpeak ~]# PATH=$PATH:/opt/command/

```

2.永久定义变量，让系统上所有用户的变量name_user的值为rocky_user。要求devops用户使用的变量name_user的值为devops，要求zhangsan用户使用的变量name_user的值为zhangsan
```bash
[root@dpeak ~]# echo "export name_user=rocky_user" >> /etc/bashrc
[root@dpeak ~]# echo $name_user
rocky_user

[root@dpeak ~]# echo "export name_user=devops" >> /home/devops/.bashrc
[root@dpeak ~]# su - devops
[devops@dpeak ~]$ echo $name_user
devops

[root@dpeak ~]# echo "export name_user=zhangsan" >> /home/zhangshan/.bashrc
[root@dpeak ~]# su - zhangshan
[zhangshan@dpeak ~]$ echo $name_user
zhangsan

```
3.为devops用户定义一个永久别名 lsuser，执行lsuser将会以长格式的方式列出devops用户家目录下的所有文件
```bash
[root@dpeak ~]# echo "alias lsuser='ls -la ~devops'" >> /home/devops/.bashrc
[root@dpeak ~]# su - devops
[devops@dpeak ~]$ lsuser
total 16
drwx------.  4 devops root 113 Aug 24 16:45 .
drwxr-xr-x. 19 root   root 250 Sep  1 16:45 ..
-rw-------.  1 devops root  27 Sep  1 16:47 .bash_history
-rw-r--r--.  1 devops root  18 Jun 17  2021 .bash_logout
-rw-r--r--.  1 devops root 141 Jun 17  2021 .bash_profile
-rw-r--r--.  1 devops root 430 Sep  1 16:49 .bashrc
drwx------.  2 devops root   6 Aug 24 16:45 .cache
drwxr-xr-x.  4 devops root  39 Aug 24 15:26 .mozilla

```

4.为所有用户定义一个永久别名cppasswd，执行cppasswd将会把/etc/passwd文件拷贝到/tmp目录下，并且以passwd-年-月-日-时-分秒格式保存
```bash
[root@dpeak ~]# echo "alias cppasswd='cp /etc/passwd /tmp/passwd-\$(date +%F-%T)'" >> /etc/bashrc
[root@dpeak ~]# bash
[root@dpeak ~]# cppasswd
[root@dpeak ~]# ls -ld /tmp/passwd-2026-09-01-16\:52\:16
-rw-r--r--. 1 root root 3471 Sep  1 16:52 /tmp/passwd-2026-09-01-16:52:16

```
5.使用su命令切换到devops用户，会读取哪些变量文件
```bash
/etc/bashrc

~/.bashrc


```

6.使用ssh命令登录到系统的root用户，会读取哪些变量文件
```bash
/etc/bashrc
/etc/profile
~/.bashrc
~/.bash_profile
```
