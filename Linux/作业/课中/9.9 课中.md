SSH和传输文件的案例:
俩台主机：A主机、B主机
1.要求你在A主机上通过SSH命令远程登陆的B主机的memeda用户
```bash

[root@dpeak ~]# ssh memeda@192.168.200.200
The authenticity of host '192.168.200.200 (192.168.200.200)' can't be established.
ECDSA key fingerprint is SHA256:oKnaZxI5JPObIfhTEv0l+bRhcWbLNb3oVAGxHGCLsyQ.
Are you sure you want to continue connecting (yes/no/[fingerprint])? yes
Warning: Permanently added '192.168.200.200' (ECDSA) to the list of known hosts.
memeda@192.168.200.200's password:
Activate the web console with: systemctl enable --now cockpit.socket

[memeda@dpeak ~]$

```
2.要求你在A主机上远程在B主机上执行id memeda命令，可以看到B主机的memeda用户信息
```bash
[memeda@dpeak ~]$ id memeda
uid=1001(memeda) gid=1001(memeda) groups=1001(memeda)

```
3.要求在A主机上，将B主机的整个/etc目录拉取到本机的/opt目录下，请你使用scp和rsync俩个命令来实现，第一次使用scp，拉取过来之后，然后删除掉，再使用rsync进行拉取
```bash

[root@dpeak ~]# scp -r root@192.168.200.200:/etc /opt/
[root@dpeak ~]# ls -ld /opt/etc/
drwxr-xr-x 148 root root 8192 Sep  9 10:18 /opt/etc/


[root@dpeak ~]# rsync -avz root@192.168.200.200:/etc /opt/
[root@dpeak ~]# ls -ld /opt/etc/
drwxr-xr-x 140 root root 8192 Sep  9 10:15 /opt/etc/
```
4.要求A主机和B主机的/tmp目录数据完全一致性，如果B主机的/tmp目录下有多出来的文件将会被删除掉，让B主机的/tmp目录的内容和A主机的/tmp目录下的内容一致
测试：请你删除A主机上的/tmp目录下的所有的文件，然后新建一个自己姓名的文件
```bash
#主机A
[root@dpeak ~]# touch /tmp/dbw
[root@dpeak ~]# ls /tmp/
dbw

[root@dpeak ~]# rsync -avz  /tmp/  root@192.168.200.200:/tmp/ --delete


#主机B
[root@dpeak ~]# ls /tmp/
anaconda.log
dbus.log
dnf.librepo.log
ks-script-1zuiwi4c
ks-script-7squv1bb
packaging.log
program.log
sensitive-info.log
ssh-xnd9fx865v
systemd-private-fc8020047fd14ef996a90432aea452d0-bluetooth.service-hpX69g
systemd-private-fc8020047fd14ef996a90432aea452d0-chronyd.service-uSuvlj
systemd-private-fc8020047fd14ef996a90432aea452d0-colord.service-Dh5VCi
systemd-private-fc8020047fd14ef996a90432aea452d0-fwupd.service-AdKYRg
systemd-private-fc8020047fd14ef996a90432aea452d0-geoclue.service-h55NIg
systemd-private-fc8020047fd14ef996a90432aea452d0-ModemManager.service-acPuXh
systemd-private-fc8020047fd14ef996a90432aea452d0-rtkit-daemon.service-45HyCf
tracker-extract-files.0
tracker-extract-files.1000
vmware-root_797-4257069498
vmware-root_806-2999526336
[root@dpeak ~]# ls /tmp/
dbw

```
5.要求将B主机的/etc/passwd文件拉取到A主机的/root目录下
```bash
[root@dpeak ~]# rsync -avz root@192.168.200.200:/etc/passwd /root/
root@192.168.200.200's password:
receiving incremental file list
passwd

sent 43 bytes  received 1,093 bytes  757.33 bytes/sec
total size is 2,594  speedup is 2.28

[root@dpeak ~]# ls -l ./passwd
-rw-r--r-- 1 root root 2594 Sep  9 10:15 ./passwd


```
6.将A主机的/root/passwd文件同步到B主机的/opt目录下，如果同步成功，则将A主机的/root/passwd文件删
除掉
```bash

[root@dpeak ~]# rsync -avz /root/passwd root@192.168.200.200:/opt --remove-source-files
root@192.168.200.200's password:
sending incremental file list
passwd

sent 1,093 bytes  received 43 bytes  757.33 bytes/sec
total size is 2,594  speedup is 2.28
[root@dpeak ~]# ls -l ./passwd
ls: cannot access './passwd': No such file or directory


```


---

前提准备：2台虚拟机
网络模式要保证一致的
将其中一台主机的主机名修改为A
另外一台主机的主机名修改为B
需求一：A主机作为跳板机的存在，可以免密登录到B主机的超管用户以及devops用户
```bash
[root@DPeak-A ~]# ssh-keygen
Generating public/private rsa key pair.
Enter file in which to save the key (/root/.ssh/id_rsa):
Enter passphrase (empty for no passphrase):
Enter same passphrase again:
Your identification has been saved in /root/.ssh/id_rsa.
Your public key has been saved in /root/.ssh/id_rsa.pub.
The key fingerprint is:
SHA256:X+s1BZETBjnYMTnEm4pbffLaYkOVuANfeXz4RM8SJis root@DPeak-A
The key's randomart image is:
+---[RSA 3072]----+
|           ===+o |
|          . Oo=..|
|             X.X.|
|          E = B.B|
|        S. *.+ =o|
|        ..o.B....|
|         o...=o  |
|        .  .+o.. |
|           .o+.  |
+----[SHA256]-----+


[root@DPeak-A ~]# ssh-copy-id -i /root/.ssh/id_rsa.pub root@192.168.200.200
[root@DPeak-A ~]# ssh-copy-id -i /root/.ssh/id_rsa.pub devops@192.168.200.200

[root@DPeak-A .ssh]# ssh root@192.168.200.200
Activate the web console with: systemctl enable --now cockpit.socket

Last login: Wed Sep  9 14:33:37 2026 from 192.168.200.128
[root@DPeak-B ~]# exit
logout
Connection to 192.168.200.200 closed.
[root@DPeak-A .ssh]# ssh devops@192.168.200.200
Activate the web console with: systemctl enable --now cockpit.socket

Last login: Wed Sep  9 14:32:10 2026 from 192.168.200.128
[devops@DPeak-B ~]$


```
需求二：A主机的devops用户可以去免密登录到B主机的devops用户
```bash
[devops@DPeak-A ~]$ ssh-keygen
Generating public/private rsa key pair.
Enter file in which to save the key (/home/devops/.ssh/id_rsa):
Enter passphrase (empty for no passphrase):
Enter same passphrase again:
Your identification has been saved in /home/devops/.ssh/id_rsa.
Your public key has been saved in /home/devops/.ssh/id_rsa.pub.
The key fingerprint is:
SHA256:sPYNTh4Ss4HUlfQyhSo+dUVYfJY7HzZTu6dW+lzstag devops@DPeak-A
The key's randomart image is:
+---[RSA 3072]----+
|    .. ooB+  .   |
|   . .. +oo +   .|
|    . =.o..o . ..|
|    . oB.o  o =. |
|   . o=.S    + +.|
|    o. * +    .oo|
|     .  + .    +=|
|              =o+|
|           E.o o+|
+----[SHA256]-----+


[devops@DPeak-A ~]$ ssh-copy-id -i ./.ssh/id_rsa.pub devops@192.168.200.200
/usr/bin/ssh-copy-id: INFO: Source of key(s) to be installed: "./.ssh/id_rsa.pub"
/usr/bin/ssh-copy-id: INFO: attempting to log in with the new key(s), to filter out any that are already installed
/usr/bin/ssh-copy-id: INFO: 1 key(s) remain to be installed -- if you are prompted now it is to install the new keys
devops@192.168.200.200's password:

Number of key(s) added: 1

Now try logging into the machine, with:   "ssh 'devops@192.168.200.200'"
and check to make sure that only the key(s) you wanted were added.


[devops@DPeak-A ~]$ ssh devops@192.168.200.200
Activate the web console with: systemctl enable --now cockpit.socket

Last failed login: Wed Sep  9 14:30:42 CST 2026 from 192.168.200.128 on ssh:notty
There were 7 failed login attempts since the last successful login.
[devops@DPeak-B ~]$

```

需求三：B主机的devops用户可以免密登录A主机的devops用户
测试：
在A主机上远程在B主机上执行id devops命令，不需要密码认证
在B主机上远程在A主机上执行id devops命令，不需要密码认证
```bash
[devops@DPeak-B ~]$ ssh-keygen
Generating public/private rsa key pair.
Enter file in which to save the key (/home/devops/.ssh/id_rsa):
Enter passphrase (empty for no passphrase):
Enter same passphrase again:
Your identification has been saved in /home/devops/.ssh/id_rsa.
Your public key has been saved in /home/devops/.ssh/id_rsa.pub.
The key fingerprint is:
SHA256:A6aUtCCTGytWU68Wx3TaFX1Y22fIKNQRyHTwDYc4ncU devops@DPeak-B
The key's randomart image is:
+---[RSA 3072]----+
|o.. o. . ++OBB=. |
|oo = o+ +.*o=OEoo|
| +. =.o= ...o =.+|
|+. . o+.   .   ..|
|o   .o  S        |
|    .    .       |
|                 |
|                 |
|                 |
+----[SHA256]-----+

[devops@DPeak-B ~]$ ssh-copy-id -i .ssh/id_rsa.pub  devops@192.168.200.128
/usr/bin/ssh-copy-id: INFO: Source of key(s) to be installed: ".ssh/id_rsa.pub"
/usr/bin/ssh-copy-id: INFO: attempting to log in with the new key(s), to filter out any that are already installed
/usr/bin/ssh-copy-id: INFO: 1 key(s) remain to be installed -- if you are prompted now it is to install the new keys
devops@192.168.200.128's password:

Number of key(s) added: 1

Now try logging into the machine, with:   "ssh 'devops@192.168.200.128'"
and check to make sure that only the key(s) you wanted were added.


[devops@DPeak-A ~]$ ssh devops@192.168.200.200 'id devops'
uid=1002(devops) gid=1002(devops) groups=1002(devops)

[devops@DPeak-B ~]$ ssh devops@192.168.200.128 'id devops'
uid=1003(devops) gid=1003(devops) groups=1003(devops)

```
需求四：要求在B主机上重新生成devops用户的公钥和私钥文件，并且对其私钥加密，密码为123，要求免密登录的时候需要验证私钥文件的密码
验证：在B主机上，把devops用户的这把私钥文件拷贝到/opt目录下，然后通过user用户使用这把私钥文件来免密登录A主机的devops用户
```bash
[devops@DPeak-B ~]$ ssh-keygen
Generating public/private rsa key pair.
Enter file in which to save the key (/home/devops/.ssh/id_rsa):
/home/devops/.ssh/id_rsa already exists.
Overwrite (y/n)? y
Enter passphrase (empty for no passphrase):
Enter same passphrase again:
Your identification has been saved in /home/devops/.ssh/id_rsa.
Your public key has been saved in /home/devops/.ssh/id_rsa.pub.
The key fingerprint is:
SHA256:Gk+MITe4nyycU+T7ortvZvp+g7aZmx7TDzCKio3TyPY devops@DPeak-B
The key's randomart image is:
+---[RSA 3072]----+
|                 |
|     .           |
|    o =          |
|     * =         |
|    . B S        |
|   o * @         |
|.o. * O.+        |
|+*.  oBBoo       |
|=.oE=#@=...      |
+----[SHA256]-----+

[devops@DPeak-B ~]$ ssh-copy-id -i .ssh/id_rsa.pub devops@192.168.200.128
/usr/bin/ssh-copy-id: INFO: Source of key(s) to be installed: ".ssh/id_rsa.pub"
/usr/bin/ssh-copy-id: INFO: attempting to log in with the new key(s), to filter out any that are already installed
/usr/bin/ssh-copy-id: INFO: 1 key(s) remain to be installed -- if you are prompted now it is to install the new keys
devops@192.168.200.128's password:

Number of key(s) added: 1

Now try logging into the machine, with:   "ssh 'devops@192.168.200.128'"
and check to make sure that only the key(s) you wanted were added.

[devops@DPeak-B ~]$ cp .ssh/id_rsa /opt/
[devops@DPeak-B opt]$ exit
logout
[root@DPeak-B ~]# chmod o+rw /opt/id_rsa
[root@DPeak-B ~]# su - user
[user@DPeak-B ~]$ ssh -i /opt/id_rsa devops@192.168.200.128
Enter passphrase for key '/opt/id_rsa':
Activate the web console with: systemctl enable --now cockpit.socket

Last login: Wed Sep  9 14:57:27 2026 from 192.168.200.200
[devops@DPeak-A ~]$

```

---

还是2台主机 A主机，B主机
1.禁止B主机的root用户远程SSH登录，请你在A主机上去SSH连接尝试。然后再使用普通用户身份去ssh登录
```bash
[root@DPeak-B ~]# vim /etc/ssh/sshd_config
PermitRootLogin no
[root@DPeak-B ~]# systemctl restart sshd

[root@DPeak-A ~]# ssh root@192.168.200.200
root@192.168.200.200's password:
Permission denied, please try again.
[root@DPeak-A ~]# ssh devops@192.168.200.200
Activate the web console with: systemctl enable --now cockpit.socket

Last login: Wed Sep  9 14:57:13 2026
[devops@DPeak-B ~]$
```
2.禁止B主机的密码认证，仅开启密钥认证-->请你在A主机上测试，SSH命令
```bash
[root@DPeak-B ~]# vim /etc/ssh/sshd_config
PasswordAuthentication no
[root@DPeak-B ~]# systemctl restart sshd

[root@DPeak-A ~]# ssh zhangsan@192.168.200.200
zhangsan@192.168.200.200: Permission denied (publickey,gssapi-keyex,gssapi-with-mic).
[root@DPeak-A ~]# ssh devops@192.168.200.200
Activate the web console with: systemctl enable --now cockpit.socket

Last login: Wed Sep  9 16:26:38 2026 from 192.168.200.128
[devops@DPeak-B ~]$

```
3.禁止xxx.xxx.xxx.1地址使用SSH登录B主机的所有用户
```bash
[root@DPeak-B ~]# vim /etc/ssh/sshd_config
DenyUsers *@192.168.200.1
[root@DPeak-B ~]# systemctl restart sshd

C:\Users\dpeak>ssh devops@192.168.200.200
devops@192.168.200.200's password:
Permission denied, please try again.

```
4.禁止A主机使用ssh登录B主机的devops用户，请你测试
```bash
[root@DPeak-B ~]# vim /etc/ssh/sshd_config
DenyUsers devops@192.168.200.128
[root@DPeak-A ~]# ssh devops@192.168.200.200
devops@192.168.200.200's password:
Permission denied, please try again.

```
5.开启A主机的空密码认证登录，请你使用SSH去登录root用户，验证结果就是直接登录，不需要密码认证也不需要密钥认证
```bash
[root@DPeak-B ~]# vim /etc/ssh/sshd_config
PermitEmptyPasswords yes

[root@DPeak-B ~]# ssh root@192.168.200.128
Activate the web console with: systemctl enable --now cockpit.socket
[root@DPeak-A ~]# passwd -d root
Removing password for user root.
passwd: Success

Last login: Wed Sep  9 16:42:26 2026 from 192.168.200.200
[root@DPeak-A ~]#

```