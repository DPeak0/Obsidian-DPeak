# 课上案例需求

准备2台rockylinux8虚拟机
一台充当web主机和dns服务器
一台充当客户端
一个是DNS的正向解析配置
一个是apache的基于域名的虚拟主机配置
要求客户端的dns地址指向dns服务器
  基于域名的虚拟主机
访问apache01.lab0.cn的时候，网站内容显示的是apache01
访问apache02.lab0.cn的时候，网站内容显示的是apache02
最后的验证截图

## 安装软件包
```bash

```